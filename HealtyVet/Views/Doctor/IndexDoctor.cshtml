@model HealtyVet.Models.DoctorModel.GetAllServicesViewModel
@{
    ViewData["Title"] = "IndexDoctor";
}

<h1>Servicii</h1>

<br />
<a href='@Url.Action("AddService", "Doctor")' class="btn btn-danger">Add services</a>
<table class="table" BORDERCOLOR=#006666>
    <thead class="thead-dark">
        <tr>

            <th style="text-align:center">
                @Html.DisplayName("Type")
            </th>
            <th style="text-align:center">
                @Html.DisplayName("Price")
            </th>
            <th style="text-align:center">
                @Html.DisplayName("Description")
            </th>

            <th style="text-align:center"> Delete service</th>
            <th style="text-align:center"> Update service</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model.Services)
        {
        <tr>

            <td style="text-align:center">

                @Html.DisplayFor(model => item.Type)

            </td>
            <td style="text-align:center">

                @Html.DisplayFor(model => item.Price)

            </td>

            <td style="text-align:center">
                @Html.DisplayFor(model => item.Description)
            </td>
            <td style="text-align:center">
                <a asp-action="DeleteService" asp-route-id="@item.Id" class="btn btn-light">Delete services</a>
            </td>
            <td style="text-align:center">
                <a asp-action="UpdateServicii" asp-route-id="@item.Id" class="btn btn-light">Update services</a>
            </td>
        </tr>

        }
    </tbody>
</table>
