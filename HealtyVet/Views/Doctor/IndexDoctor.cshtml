@model HealtyVet.Models.DoctorModel.GetAllServicesViewModel
@{
    ViewData["Title"] = "IndexDoctor";
}

<h1>Servicii</h1>

<br />
<a href='@Url.Action("AddService", "Doctor")' class="btn btn-danger">Adauga servicii</a>
<table class="table" BORDERCOLOR=#006666>
    <thead class="thead-dark">
        <tr>

            <th style="text-align:center">
                @Html.DisplayName("Tip")
            </th>
            <th style="text-align:center">
                @Html.DisplayName("Pret")
            </th>
            <th style="text-align:center">
                @Html.DisplayName("Descriere")
            </th>

            <th style="text-align:center"> Sterge serviciu</th>
            <th style="text-align:center"> Modifica serviciu</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model.Services)
        {
        <tr>

            <td style="text-align:center">

                @Html.DisplayFor(model => item.Type)

            </td>
            <td style="text-align:center">

                @Html.DisplayFor(model => item.Price)

            </td>

            <td style="text-align:center">
                @Html.DisplayFor(model => item.Description)
            </td>
            <td style="text-align:center">
                <a asp-action="DeleteService" asp-route-id="@item.Id" class="btn btn-light">Sterge servicii</a>
            </td>
            <td style="text-align:center">
                <a asp-action="UpdateServicii" asp-route-id="@item.Id" class="btn btn-light">Modifica servicii</a>
            </td>
        </tr>

        }
    </tbody>
</table>
